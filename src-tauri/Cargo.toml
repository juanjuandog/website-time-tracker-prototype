[package]
name = "orbitlog"
version = "0.1.0"
description = "Local-first website and app time journal"
authors = ["local"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api", "tray-icon"] }
